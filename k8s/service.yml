apiVersion: v1
kind: Service
metadata:
  name: poke
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "poke"
    port: 8080
  selector:
    app: poke

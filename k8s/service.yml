apiVersion: v1
kind: Service
metadata:
  name: reet
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "reet"
    port: 8080
  selector:
    app: reet
